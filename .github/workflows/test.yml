# .github/workflows/test.yml
name: Python Tests and Checks

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    - name: Set up Python 3.x
      uses: actions/setup-python@v5
      with:
        python-version: '3.x'

    # 🚨 YENİ ADIM: XVFB (Sanal Ekran) Kurulumu
    - name: Install Xvfb and start virtual display (Solves Exit Code 134)
      run: |
        sudo apt-get update
        sudo apt-get install -y xvfb
        # Sanal ekranı başlat
        Xvfb :99 -screen 0 1024x768x24 & 
        echo "DISPLAY=:99" >> $GITHUB_ENV 

    - name: Install dependencies and set up package
      run: |
        python -m pip install --upgrade pip
        # PyQt5 ve test kütüphaneleri dahil
        pip install -r requirements.txt 
        # Paketi kur
        pip install -e . 
        
    - name: Run tests with pytest
      # DISPLAY=:99 değişkeni sayesinde PyQt5 çökmez
      run: |
        pytest
      # PyQt5'in testlerde görünür olması için DISPLAY değişkeni artık GitHub tarafından otomatik olarak iletilecek.
      run: |
        pytest
